# Phoneme CTC training boundary

Training code must be added through a reviewed export adapter after the Tamil phoneme inventory,
dataset licence, participant-level split, and approved checkpoint are known. The first supported
candidate is an AI4Bharat Tamil IndicConformer teacher/baseline with a separately trained phoneme
CTC head. Its existing text-ASR vocabulary must not be presented as the final phoneme head.

